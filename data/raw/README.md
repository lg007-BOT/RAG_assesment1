# Add source PDFs here

Place the PDF document set for the assignment in this folder, then run:

```bash
python -m src.ingest
```

Use one focused document set of at least 20 pages. Keep the original PDF so that
the page numbers returned in citations can be checked during the demo.
